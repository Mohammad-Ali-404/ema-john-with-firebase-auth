/**
 * 1. create firebase project
 * 2. create web app
 * 3. npm i firebase
 * 4. save firebase config and export default app
 * 5. Build > Authentication > get started  > enable sign in method  
 * 6. create sign up and login route 
 **/

/**
 * 1. create a context provider
 * 2. create a context and set provider
 * 3.set children props
 * 4. set context value
 * 5. set the provider
 **/
 /**
  * HOSTING
  * --------------------
  * One time per pc
  * 1. npm install -g firebase-tools
  * 2. firebase login
  * ---------------------
  * 
  * For Each project one time
  * 1. firebase init
  * 2.procced
  * 3.firbase hosting :(up and down arrow) use space bar to select
  * 4. exsisting project 
  * 5. select the project careful 
  * 6. which project as public directory : dist
  * 7. single page application : yes
  * 8. continuous deployment : no
  * ---------------------------------
  * 
  * Every Time Deploy
  * 1. npm run build
  * 2. firebase deploy
  **/